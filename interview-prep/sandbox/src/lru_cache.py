# Part 1 — Base Implement an LRU (least-recently-used) cache with a fixed capacity:
#
# get(key) returns the value if present, else -1 (or None), and marks the key as most recently used.
# put(key, value) inserts or updates. If the cache is at capacity, evict the least recently used key first.
# Both operations must be O(1).
#
# State your data structures before coding. Be ready to explain why a plain dict or a plain list each fail one of the operations.
class LRUCache:
    def __init__(self) -> None:
        pass
